# API调用日志表 - 测试数据生成脚本

## 概述

用于生成 `api_call_log` 表的 300 万条测试数据，用于性能验证。

## 数据特征

- **总记录数**: 3,000,000 条
- **数据分布**:
  - API配置ID: 1-100 之间随机
  - 用户ID: 1-10000 之间随机
  - 响应状态: 80% 成功，20% 失败
  - 调用时间: 最近30天内的随机时间
  - 执行耗时: 50-5000 毫秒之间

## 生成方式

### 方式一：Python 脚本（推荐，性能最好）

**优点**:
- 性能最优，速度快
- 实时进度显示
- 可以灵活调整批次大小
- 自动验证生成结果

**使用步骤**:

1. 安装依赖:
```bash
pip install pymysql
# 或者
pip install mysql-connector-python
```

2. 修改数据库配置（如需要）:
编辑 `generate_test_data.py`，修改 `DB_CONFIG` 字典中的连接信息。

3. 运行脚本:
```bash
python3 generate_test_data.py
```

**预计耗时**: 5-15 分钟（取决于服务器性能）

**输出示例**:
```
============================================================
开始生成 api_call_log 表测试数据
============================================================
总记录数: 3,000,000
批次大小: 10,000
预计批次数: 300
============================================================
✓ 数据库连接成功: 192.168.16.122:3308
[14:30:25] 已插入:    100,000 / 3,000,000 ( 3.33%) | 速度:   25,000 条/秒 | 预计剩余: 116 秒
[14:30:29] 已插入:    200,000 / 3,000,000 ( 6.67%) | 速度:   25,000 条/秒 | 预计剩余: 112 秒
...
```

---

### 方式二：SQL 存储过程

**优点**:
- 不需要安装额外的 Python 依赖
- 纯 SQL 执行

**缺点**:
- 速度较慢
- 无实时进度显示

**使用步骤**:

1. 连接到 MySQL:
```bash
mysql -h 192.168.16.122 -P 3308 -u test_medical_data_management -p
```

2. 执行脚本:
```bash
source generate_test_data.sql
# 或者
mysql -h 192.168.16.122 -P 3308 -u test_medical_data_management -p test_medical_data_management < generate_test_data.sql
```

**预计耗时**: 15-30 分钟

---

### 方式三：直接 SQL 批量插入（快速测试少量数据）

如果只想快速测试少量数据（如 10 万条），可以直接执行简单的 INSERT 语句:

```sql
USE test_medical_data_management;

INSERT INTO api_call_log (
    api_config_id, api_path, token, user_id, request_params,
    execute_sql, response_status, response_result, error_msg,
    execution_time, call_time
)
SELECT
    FLOOR(1 + RAND() * 100),
    CONCAT('/api/v', FLOOR(1 + RAND() * 3), '/', 
           CASE FLOOR(RAND() * 5) WHEN 0 THEN 'user' WHEN 1 THEN 'order' 
           WHEN 2 THEN 'product' WHEN 3 THEN 'payment' ELSE 'medical' END,
           '/', CASE FLOOR(RAND() * 3) WHEN 0 THEN 'query' 
           WHEN 1 THEN 'create' ELSE 'update' END),
    CONCAT(SUBSTRING(MD5(RAND()), 1, 32)),
    CAST(FLOOR(1 + RAND() * 10000) AS CHAR),
    CONCAT('{"patient_id":', FLOOR(1000 + RAND() * 9000), '}'),
    CONCAT('SELECT * FROM medical_records WHERE id = ', FLOOR(1000 + RAND() * 9000)),
    IF(RAND() < 0.8, 1, 0),
    IF(RAND() < 0.8, '{"code":200,"message":"success"}', NULL),
    IF(RAND() >= 0.8, '数据库连接超时', NULL),
    FLOOR(50 + RAND() * 4950),
    DATE_SUB(NOW(), INTERVAL FLOOR(RAND() * 30) DAY)
FROM (
    SELECT a.N + b.N * 10 + c.N * 100 + d.N * 1000 AS num
    FROM 
        (SELECT 0 AS N UNION SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9) a,
        (SELECT 0 AS N UNION SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9) b,
        (SELECT 0 AS N UNION SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9) c,
        (SELECT 0 AS N UNION SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9) d
) numbers
WHERE numbers.num < 100000;
```

---

## 性能优化建议

### 在生成数据前优化数据库

```sql
-- 1. 临时禁用索引（生成数据后重建）
ALTER TABLE api_call_log DISABLE KEYS;

-- 生成数据...

-- 重建索引
ALTER TABLE api_call_log ENABLE KEYS;

-- 2. 增加 innodb_buffer_pool_size（如果使用 InnoDB）
-- 在 my.cnf 中设置: innodb_buffer_pool_size = 2G

-- 3. 关闭 binlog（仅测试环境）
SET SESSION sql_log_bin = 0;

-- 4. 增加批量提交大小
SET SESSION autocommit = 0;
```

### Python 脚本优化参数

如果服务器内存充足，可以增加批次大小以提升速度：

```python
BATCH_SIZE = 20000  # 从 10000 增加到 20000
```

如果服务器内存较小，可以减少批次大小：

```python
BATCH_SIZE = 5000   # 从 10000 减少到 5000
```

---

## 验证生成的数据

执行以下 SQL 验证数据：

```sql
-- 总记录数
SELECT COUNT(*) AS total_records FROM api_call_log;

-- 数据分布
SELECT 
    COUNT(*) AS total_records,
    MIN(call_time) AS earliest_time,
    MAX(call_time) AS latest_time,
    COUNT(DISTINCT user_id) AS distinct_users,
    COUNT(DISTINCT api_config_id) AS distinct_api_configs,
    SUM(CASE WHEN response_status = 1 THEN 1 ELSE 0 END) AS success_count,
    SUM(CASE WHEN response_status = 0 THEN 1 ELSE 0 END) AS failure_count,
    AVG(execution_time) AS avg_execution_time,
    MAX(execution_time) AS max_execution_time,
    MIN(execution_time) AS min_execution_time
FROM api_call_log;

-- 按日期分布
SELECT 
    DATE(call_time) AS date,
    COUNT(*) AS count
FROM api_call_log
GROUP BY DATE(call_time)
ORDER BY date DESC;

-- 按API配置ID分布（前10）
SELECT 
    api_config_id,
    COUNT(*) AS count
FROM api_call_log
GROUP BY api_config_id
ORDER BY count DESC
LIMIT 10;
```

---

## 清理测试数据

如果需要重新生成数据，可以先清理：

```sql
-- 方式1: 删除所有数据（保留表结构）
TRUNCATE TABLE api_call_log;

-- 方式2: 删除并重置自增ID
DELETE FROM api_call_log;
ALTER TABLE api_call_log AUTO_INCREMENT = 1;

-- 方式3: 删除指定范围的数据
DELETE FROM api_call_log WHERE id BETWEEN 1 AND 3000000;
```

---

## 故障排查

### Python 脚本连接失败

1. 检查数据库连接信息是否正确
2. 检查网络连接和防火墙设置
3. 确认数据库用户权限

### 生成速度慢

1. 检查数据库服务器性能
2. 增加 `BATCH_SIZE` 参数（如果内存充足）
3. 临时禁用索引：`ALTER TABLE api_call_log DISABLE KEYS;`
4. 关闭 binlog（仅测试环境）

### 内存不足

1. 减少 `BATCH_SIZE` 参数
2. 增加数据库服务器的内存
3. 分批生成数据（先生成 100 万，再生成 100 万）

---

## 注意事项

1. **生产环境**: 此脚本仅用于测试环境，不要在生产环境执行
2. **磁盘空间**: 确保有足够的磁盘空间（300万条记录大约需要 2-5GB）
3. **备份**: 生成数据前建议备份数据库
4. **性能影响**: 生成数据期间可能会影响数据库性能，建议在低峰期执行


